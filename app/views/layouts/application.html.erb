<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %> | Stitch in Time</title>
    <%= render 'layouts/rails_default' %>
    <%= render 'layouts/shim' %>
  </head>

  <body>
    <%= render 'layouts/header' %>
    <div class="container">
      <%= render 'layouts/devise_notice' %>
      <%= yield %>
    </div>
    <%= render 'layouts/footer' %>
  </body>
</html>
