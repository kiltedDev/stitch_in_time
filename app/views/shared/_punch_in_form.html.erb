<div class="col-12 text-center">
  <%= button_to punches_path(project_id: @project, method: :post), class:"btn btn-success btn-circle btn-xl" do%>
  <i class="fa fa-hourglass-start"></i>
  <% end %>
</div>
