<%= form_for(@punch) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <%= f.label :comment %>
  <%= f.text_field :comment, class: 'form-control', placeholder: @comment %>

  <%= f.datetime_select :comment, class: 'form-control', placeholder: @comment %>
<% end %>
